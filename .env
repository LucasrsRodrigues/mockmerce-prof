# ---- Deploy (EasyPanel / Docker) ----
# Este painel é servido por Nginx e faz proxy de /api -> BACKEND_URL.
# BACKEND_URL é uma variável de RUNTIME do container (não do Vite):
# aponte para a URL interna do serviço do backend na mesma stack do EasyPanel.
BACKEND_URL=http://backend:3333
